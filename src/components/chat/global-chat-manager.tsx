// This component is no longer used - chat functionality moved to sidebar
// Keeping this file for backward compatibility, but it renders nothing

const GlobalChatManager = () => {
  return null;
};

export default GlobalChatManager;
